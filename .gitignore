# Secrets (Tavily key etc.) -- root-level only, NOT elibrary/app/config.py
/config.py

# Logs & temp images
*.log
brave_screenshot_localhost.png

# Python
__pycache__/
*.pyc

# Node / vendor cache (not needed for build; copied as curated vendor files)
**/node_modules/
